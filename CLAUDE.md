@AGENTS.md

<!--
CLAUDE.md — входной указатель, не место для правил.
Единственный источник правил — AGENTS.md (импортирован выше). Не дублировать их здесь.
-->

## Порядок чтения перед ответом

1. `index.md` — живое оглавление: найти релевантные разделы.
2. `STATUS.md` — что уже сделано, что в работе, известные проблемы.
3. Нужный документ в `docs/` — по ссылке из `index.md`.
4. Код в `App/` — только нужные файлы, не сканировать всё подряд.

Если данных нет ни в `docs/`, ни в коде — сказать об этом прямо, не додумывать.
